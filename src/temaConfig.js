import React from 'react';
import {createTheme} from '@mui/material'

const tema = createTheme({
    palette:{
        primary: {
            main: '#000'
        }
    }
        
})

export default tema